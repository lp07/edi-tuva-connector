name: 'edi_tuva_connector'
version: '1.0.0'

profile: 'edi_tuva_connector'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  edi_tuva_connector:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    input_layer:
      +materialized: table

vars:
  claims_enabled: true